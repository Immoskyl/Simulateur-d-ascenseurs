package ascenseur.traitement;

public class RequeteExterne extends Requete {

    private boolean direction;

    public RequeteExterne(int etage, boolean direction) {
        super(etage);
        this.direction = direction;
    }

    public boolean getDirection() {
        return direction;
    }
}
